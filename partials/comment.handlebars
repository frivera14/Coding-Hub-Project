<div>
    {{#each this}}
    <div class="card bg-light p-2 bg-opacity-75 container w-75">
        <p class="card-text fs-5">{{comment_text}}</p>
        <p class="card-text fs-6 blockquote-footer">{{user.username}} on {{format_date created_at}}</p>
    </div>
    <br>
    {{/each}}
</div>