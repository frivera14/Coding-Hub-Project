<section>
    <div class="d-flex justify-content-between">
         <p class="h5 text-dark card-title">{{title}}</p>
    </div>
    <p class="text-muted">{{user.username}} on {{format_date created_at}}</p>
    <p class="card-text">{{pub_text}}</p>
    <a href="/pub/{{id}}"><p class="text-muted">{{comments.length}} Comments</p></a>
</section>
    </a>
